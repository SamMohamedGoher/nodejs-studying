// Event module

/*

const EventEmitter = require(`events`);

class LisLogger extends EventEmitter{
    logger(x) {
        console.log(x);
        this.emit(`IDs`, [1234, 1235, 1326, 1237]);
    }
}

module.exports = LisLogger;

*/

